import React from 'react';
import {makeStyles} from "@material-ui/styles";

const useStyle = makeStyles({

});

export default function ReviewPlaces() {
    const classes = useStyle();

    return <div>
        no content
    </div>;
};