
class Task {
    constructor(id, name, tileBasedData, minZoom) {
        this.id = id;
        this.name = name;
        this.tileBasedData = tileBasedData;
        this.minZoom = minZoom;
    }
}

export default Task;